📌 Теги:  #AI #math
⏳ Время создания:  2025-11-03 18:57

---
Градиент - это вектор, который указывает направление наибольшего возрастания функции в данной точке:

![[38.png]]

В нейросетях градиент используется для оптимизации весов (обучения) модели. Каким образом это происходит? Предположим, существует функция L($\theta$), которая зависит от параметров сети $\theta$ (весов и смещений).  Мы хотим уменьшить ошибку, то есть найти такие $\theta$, чтобы L($\theta$) была минимальной. Для этого используется градиентный спуск. То есть мы идём по функции в направлении обратном градиенту (антиградиент). Таким образом $\large w_{new} = w_{old}-\mu*(-\nabla L(\theta))$ 