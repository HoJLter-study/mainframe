# MOC: История

## Все заметки с #history

```dataviewjs
````

```dataviewjs
let pages = dv.pages('#history')
    .sort(p => p.file.name, 'asc');

dv.list(pages.map(p => {
    let link = p.file.link;
    let inlinksCount = p.file.inlinks.length;
    return `${link} (${inlinksCount} связей)`;
}));
````

